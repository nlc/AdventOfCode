> (':'cut]) each cutLF (': *';':') rxrplc fread 'sample.txt'
>@([: , each ' ' ({."1@rxmatches ]) each {:) each (':'cut]) each cutLF (': *';':') rxrplc fread 'sample.txt'
('+*' {~ [: #: [: i. 2 ^ #) 1 pick >@([: , each ' ' ({."1@rxmatches ]) each {:) each (':'cut]) each cutLF (': *';':') rxrplc fread 'input.txt'
